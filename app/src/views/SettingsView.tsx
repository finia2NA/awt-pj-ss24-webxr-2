import SettingsWindow from "../windows/SettingsWindow";

/**
 * SettingsView Component
 * 
 * This functional component renders the SettingsWindow component.
 * It serves as a wrapper to display the settings interface.
 *
 * @component
 * @returns {JSX.Element} JSX element representing the settings view.
 */
const SettingsView = () => {
  return (
    <SettingsWindow />
  );
}

export default SettingsView;
