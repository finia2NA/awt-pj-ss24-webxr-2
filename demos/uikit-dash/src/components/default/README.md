When we use e.g. `npx uikit component add default accordion`, files are created in here to enable us to use the component in our project.
This is only for the *default* kit.

See [uikit documentation](https://docs.pmnd.rs/uikit/kits/default) for more information.